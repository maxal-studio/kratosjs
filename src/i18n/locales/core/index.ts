// Core SERVER catalog assembled as `{ [locale]: catalog }` for the `core` namespace.
import en from './en';
import sq from './sq';

export { en as coreEn, sq as coreSq };

/** Core server framework strings, keyed by locale (the `core` namespace). */
export const coreResources = { en, sq };
