import en from './en';
import sq from './sq';

/** Client UI-chrome catalog, keyed by locale (the `core` namespace). */
export const clientCoreResources: Record<string, Record<string, string>> = { en, sq };
